name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: "0 0 * * *"

jobs:
  ci:
    uses: josa42/gha-workflows/.github/workflows/ha-integration.yml@main
